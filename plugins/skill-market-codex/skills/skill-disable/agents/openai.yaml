interface:
  display_name: "Skill Market Disable"
  short_description: "Disable a package while keeping its files"
  default_prompt: "Use $skill-disable to disable this package while preserving its files."
policy:
  allow_implicit_invocation: false
