interface:
  display_name: "Parallel Tasks"
  short_description: "Fan out independent subtasks to parallel agents with complexity-based model routing"
  default_prompt: "Use $parallel-tasks to split this task into independent subtasks, route each subtask to the right model tier by complexity, run them on parallel agents, and integrate the validated results."
