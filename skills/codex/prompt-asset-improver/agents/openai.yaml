interface:
  display_name: "Prompt Asset Improver"
  short_description: "Improve durable agent prompt assets"
  default_prompt: "Use $prompt-asset-improver to confirm scope and proposed changes, refresh inventory, back up confirmed editable changes, align paired assets and related metadata, then validate the prompt-asset changes."
