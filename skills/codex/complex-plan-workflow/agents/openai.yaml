interface:
  display_name: "Complex Plan Workflow"
  short_description: "Plan cross-session, cross-boundary, uncertain, or rollback-sensitive work"
  default_prompt: "Use $complex-plan-workflow before coding when work spans sessions, crosses boundaries, needs evidence/RFC decisions, or requires durable handoff, rollback, or isolation planning."
