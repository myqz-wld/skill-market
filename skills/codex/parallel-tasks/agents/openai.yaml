interface:
  display_name: "Parallel Tasks"
  short_description: "Fan out independent files, modules, audits, research tracks, or tests to parallel agents"
  default_prompt: "Use $parallel-tasks when this work can split into 2+ independent tracks with disjoint write sets: decompose them, route each by complexity, run parallel agents, and integrate validated results."
